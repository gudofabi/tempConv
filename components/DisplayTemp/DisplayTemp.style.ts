import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  root: {
    flexDirection: "row",
  },
  tempText: {
    fontSize: 90,
    color: "white",
    fontWeight: 700,
  },
  unit: {
    fontSize: 25,
    fontWeight: 700,
    color: "white",
  },
});
